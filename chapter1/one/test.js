import { unique, uniqueNoStructs } from './index';

test('unique function is defined', () => {
  expect(unique).toBeDefined()
})

test('uniqueNoStructs function is defined', () => {
  expect(uniqueNoStructs).toBeDefined()
})